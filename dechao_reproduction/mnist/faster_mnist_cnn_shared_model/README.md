# faster_mnist_cnn_shared_model

Shared frozen CNN assets for MNIST embedding experiments: checkpoint, training logs/curves, Python feature extractor, and MNIST gz files used for training/evaluation.
